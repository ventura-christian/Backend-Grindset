"""
Task: Write a python function named filter_even_numbers(numbers) that takes a list of integers and returns a new list containing only the even numbers from the input list.
COMPLETE - THIS NEEDS TO BE FINISHED, THE CODE BELOW ARE ALL INCOMPLETE.
"""

"""def filter_even_numbers(numbers):
    numbers = []
    new_list = []
    for num in numbers:
        if num % 2 != 0:
            continue
        else:
            new_list.append(num)"""

"""numbers = [90, 103, 8, 56, 77, 834, 23, 3, 5, 10]
even_numbers = []
for num in numbers:
    if num % 2 == 0:
        even_numbers.append(num)
print(even_numbers)"""

"""numbers = [90, 103, 8, 56, 77, 834, 23, 3, 5, 10]
def filter_even_numbers(numbers):
    
    for num in numbers:
        even_numbers = []
        if num % 2 == 0:
            even_numbers.append(num)
    print(even_numbers)
    
filter_even_numbers(numbers)"""
